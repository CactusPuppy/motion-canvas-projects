import { makeProject } from "@motion-canvas/core";

import thumbnail from "../../scenes/demon-lord/demon_lord_thumbnail?scene";

import '../../global.css';

export default makeProject({
  scenes: [thumbnail]
});
